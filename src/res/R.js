import strings from './strings'
import images from './images'
import colors from './colors'
import size from './size'

const R = {
    strings,
    images,
    colors,
    size,
}

export default R;